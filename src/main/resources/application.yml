server:
    servlet:
        context-path: /api

spring:
    servlet:
        multipart:
            max-file-size: 100MB
            max-request-size: 100MB 
    application:
        name: safety

logging:
    level:
        org.hibernate.SQL: debug
        org.hibernate.type: trace

firebase:
    name: safe
    id: safe-a112d
    number: 677135268696